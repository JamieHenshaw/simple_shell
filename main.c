#include "main.h"

int main(int argc, char **argv, char **env)
{
	UNUSED(argc);
	data_shell datash;

	datash.av = argv
	datash._environ = env;
	datash.status = 0;
	datash counter = 0;
	
	while(1)
		return (datash.staus);
}
